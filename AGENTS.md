# Active remaining-fidelity completion (2026-09-16)

The user explicitly reopened remaining feasible fidelity work after the September13
scope correction. This supersedes the prior completion paragraph's prohibition on
new paid experiments within this newly requested work. Work under
`paper_runs/fidelity_completion_v2` and private `artifacts/fidelity_completion_v2`;
read its protocol and handoff. Preserve every earlier study and sealed forecast.
All calls retain the same450USD project soft stop/500USD hard ceiling, including
prior spending and uncertain reservations. No commits, pushes, source-author
contact or fabricated human review is authorized. Track all69 papers separately;
execute recoverable mechanisms and explicitly document unavailable inputs/code/
checkpoints/budget-limited work. Never relabel a source component, adapted run or
surrogate as a faithful full-paper reproduction. The old forecast has now been
settled without changing its commitment and without any new model call.

# Publication upgrade: historical evidence complete (2026-09-09)

All six approved cases, matched controls, calibration and final analysis are
complete under `paper_runs/publication_upgrade_v1`; the18-page manuscript passed
numeric and visual review. FinMem538, FinAgent544 and TradingAgents61 records
match their full cached replays exactly. No paid native job remains necessary.
Read `docs/PUBLICATION_UPGRADE_RUNBOOK.md`, the current milestones/package manifest,
and private `artifacts/publication_upgrade_v1/HANDOFF.md` for provenance.

All original studies and EFS pinned code/results remain frozen. Do not replace
or alter the sealed prospective forecast, or invent human review/future returns.
Independent human source review and the prospective pilot outcome remain pending.
The six primary comparisons are inconclusive at the stated economic margin, not
proof of no alpha or full original-paper reproduction. All paid usage and missing-
response reservations remain in the canonical ledger. No further experiments,
paid runs, commits, pushes, PRs, author contact or submission are authorized by
this completed research-upgrade request. Archive verification is separately
recorded in package_manifest.json after both bundles and copies are checked.

# Active economic fidelity repair (2026-09-06)

The first EFS v1 daily LLM pilot is complete and independently accounted, with
290 generation steps, 1,510 daily observations, and a byte-identical full cached
replay. Read `paper_runs/economic_fidelity/efs_v1/REPORT.md` and its outcome-free
`SOURCE_REVIEW_PACKET.md`. The broader fidelity cohort and later publication
stages remain open; human source review is pending. Use
`scripts/replay_efs_economic_fidelity.py` for reproduction without paid calls.
The canonical API ledger/cache is under `runs/economic_fidelity/openrouter`.

The user has activated the six publication improvements sequentially, beginning
with economic fidelity, and authorized metered OpenRouter LLM calls. Read
`docs/ECONOMIC_FIDELITY_PROTOCOL.md`. Preserve all earlier studies. The new work
must restore source cadence, inputs, portfolio rules, and actual agent mechanisms
where feasible. The September 4 preference for deterministic substitutes governs
the completed in-spirit study, not this new repair. Do not claim independent
human review or full-paper reproduction without evidence. Do not commit or push
unless explicitly requested.

# Current research objective (2026-09-04)

The strict 69-paper headline audit and common-transfer study is complete and
frozen under `paper_runs/us_jkp_headline`. The user has now authorized a separate
best-effort, in-spirit reconstruction for every paper except the seven whose
native task is structurally incompatible with monthly U.S. stocks. Read
`docs/US_JKP_IN_SPIRIT_PROTOCOL.md` and
`paper_runs/us_jkp_in_spirit/milestones.json` before continuing research.

- Preserve the completed strict ledger, results, and claim boundaries. Do not
  overwrite or relabel strict `closed_not_evaluable` verdicts.
- Work on one in-spirit milestone at a time. The new study carries 17 existing
  common evaluations, discards exactly seven structural mismatches, and targets
  transparent reconstructions for the remaining 45 papers.
- Extract the central mechanism from the paper or source, then implement a
  researcher-authored surrogate using available JKP inputs. Missing code,
  checkpoints, prompts, traces, or final factors are no longer blockers by
  themselves: make the smallest explicit choices needed to instantiate the idea.
- Every reconstruction must distinguish preserved, approximated, and invented
  elements and is labelled `in_spirit_reconstruction`, never faithful/native.
- Fix one paper-specific recipe before inspecting its JKP return. Do not try many
  proxies and promote the best performer.
- Evaluate the complete fixed monthly path and common costs/JKP attribution. A
  negative or insignificant result is a completed reconstruction.
- Prefer interpretable deterministic surrogates. Use chronological training only
  when learning/search is central, and keep the final common evaluation outside
  tuning and model selection.
- Do not use future-selected holdings, future recommendation labels, or result
  curves as formation inputs. Historical outputs may guide mechanism selection
  but cannot be replayed outside their disclosed dates.
- Close a new milestone only with a runnable strategy, tests, returns, manifest,
  and a verdict. The seven discarded papers need no manufactured proxy.
- Batch full Python 3.12/3.9 validation, one push, and one CI run after every ten
  new reconstructions and at final delivery; continue focused validation and
  local commits between batches to avoid notification noise.

The older strict-study rules below remain as provenance for the frozen study,
not as the operating threshold for the new in-spirit study.

- Work on one of the 69 paper milestones at a time.
- Extract the central proposed strategy from the paper or its code. Preserve its
  economic/algorithmic mechanism while documenting the U.S./monthly/JKP adapter.
- Evaluate the best defensible implementation or central partial implementation;
  do not require recreation of every original result, ablation, seed, or API.
- A negative or insignificant JKP result is a completed experiment, not a reason
  to search for a more favorable specification.
- Close a genuinely non-evaluable paper with concrete evidence and a bounded
  effort record, then move on. Do not manufacture a generic factor proxy and
  attribute it to the paper.
- Reuse existing source audits. Do not restart exhaustive fork/history searches,
  paper-layout reconstruction, or all-table conformance unless a specific missing
  item is necessary for the active headline implementation.
- Keep milestone closure, evaluated headline strategies, evaluated partial
  strategies, and unavailable cases as separate counts. Historical "full paper
  reproduced" flags and unit-test totals are not the new progress measure.
- Preserve old studies and claim boundaries as provenance. Do not relabel old
  proxies, checkpoints, or author-output verification as new strategy results.
- All real work and execution remain on Bouchet in this canonical checkout.
  Keep licensed JKP security-level data and derived holdings out of public Git.

The September 3 user direction supersedes the earlier open-ended requirement to
reproduce every original paper experiment before moving to the next paper.
