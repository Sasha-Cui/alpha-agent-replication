# M023: EFS: Evolutionary Factor Searching for Sparse Portfolio Optimization Using Large Language Models disclosed component

Status: **completed partial monthly U.S./JKP evaluation**, not the LLM-guided evolutionary factor search followed by factor scoring and sparse portfolio optimization.

The literal published Table VI US50 skew-gated breakout factor is evaluated from the fixed source formula `cs_zscore((ts_max(prices,30)-ts_decay_linear(prices,21))/((ts_std(returns,3)+ts_std(returns,30))*if_else(ts_skew(returns,14)<0,ts_std(returns,7),ts_std(returns,21))))`. Preserved: published US50 formula tree; 30-period maximum; 21-period linear decay; 3/7/14/21/30-period volatility and skew gate; cross-sectional z-score. Adapted: daily US50 bars to monthly top-1,000 U.S. JKP bars; researcher portfolio; common costs and benchmark. The disclosed monthly adapter is: inputs: JKP monthly abs(prc) and ret in the formation-date top-1,000 U.S. universe; operators: source tree with sample rolling std, linear decay weights 1..n, conditional skew volatility gate, and monthly population cs_zscore; portfolio: researcher top-10 equal-weight long-only score portfolio; costs: common fixed cost cases with 10 bp primary; missing_returns: fixed holdings re-accounted under common zero and adverse-100 policies without reranking. This component was already mapped and evaluated before the new study, so this result is exploratory rather than newly outcome-blind.

At 10 bp one-way costs, the 305-month path has CAGR 8.41%, annualized Sharpe 0.614, and maximum drawdown -44.13%. The 185-month rolling JKP133 residual mean is 0.60% annually (HAC t=0.380, p=0.7036; descriptive 69-test bound=1.0000).

The result is performance of one disclosed formula component after explicit adaptations. It does not reproduce or claim: evolutionary search; LLM generation; Scores-to-Weights or RW; sparse optimizer; native portfolio; paper result. It must not be used as evidence that the full paper system worked or failed.
