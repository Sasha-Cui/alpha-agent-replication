# M051: Hubble safe and diverse alpha discovery

Status: **closed not evaluable on monthly U.S./JKP data**.

Hubble's central strategy is the selected top-five output of its constrained LLM/RAG/AST factor-mining pipeline. The current v2 paper names the winning families—two range, two volatility, and one trend factor—but explicitly withholds their formulas and hyperparameters. No attributable runtime, operator registry, prompts/responses, RAG corpus/index, candidate records, universe/data snapshot, factor values, selection artifacts, or raw result arrays are public.

Family labels and bar heights do not identify a monthly score. Substituting an arbitrary range, volatility, or trend factor would bypass the defining search and family-selection mechanism and invite outcome-based cherry-picking. The executed local AST fixture proves only that a conditional sandbox design can run; it is not a Hubble factor or result.

The chronology is also unresolved: the two named v2 LLM backends became public after the claimed OOS window began, while no timestamped formula freeze exists. V2 reports simple long-short diagnostics without transaction costs or a complete investable portfolio rule.

No return path is fabricated. This closure does not prove the paper's positive top-factor evidence false; it records that the intentionally hidden formulas prevent causal transfer or independent testing. Reopen when the top-five formulas or attributable persisted artifacts are released.
