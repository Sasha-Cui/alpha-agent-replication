# Alpha-Jungle — LLM-Powered MCTS for Formulaic Factor Mining JKP/USA scope decision

## Decision

No repository-level metric is computable because no confirmed public codebase is available for this paper in the registry.

Under the current rule, valid return experiments must use only the approved read-only USA inputs:

- `${ALPHA_EVOLVE_JKP_ROOT}`
- `${ALPHA_EVOLVE_RETURN_DATA_ROOT}`

A paper with no confirmed code repository and no approved-input candidate return stream cannot be assigned a Sharpe ratio, appraisal/information ratio, alpha t-statistic, GRS statistic, or FF5Mom beat verdict without inventing a strategy.
